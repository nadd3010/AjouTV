module top (
    input  wire       clk,
    input  wire       rst_n,
    input  wire       rx,
    output wire       tx,
    output wire [3:0] led,
    output wire       buzzer
);

// TODO: 전체 모듈 인스턴스 연결

endmodule
